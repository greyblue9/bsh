public static void ll(String path) { File[] files = new File(path).listFiles(); Arrays.sort(files, cmp = new Comparator() { public int compare(Object o1, Object o2) { return Long.valueOf(((File)o1).lastModified()).compareTo(Long.valueOf(((File)o1).lastModified())); } public boolean equals(Object that) { return System.identityHashCode(that) == System.identityHashCode(this); } }); String[] lines = new String[files.length]; int fidx = -1, len = files.length; File lsfile = null; while (++fidx < len && (lsfile = files[fidx]) != null) lines[fidx] = String.format("%9d %s %s", lsfile.length(), DateUtil.format(lsfile.lastModified(), "yyyy-MM-dd HH:mm:ss"), lsfile.getName()); System.out.println(StringUtils.join(lines, "\n")); System.out.printf(" - %s: %d items -\n", path, files.length); }